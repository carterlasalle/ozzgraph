{
  "name": "ozzgraph-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Optional OzzGraph dashboard — reads kernel run state (actions.jsonl, graph.db, artifacts) via a read-only JSON API. Outside the competition image.",
  "license": "UNLICENSED",
  "type": "module",
  "packageManager": "yarn@4.18.0",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "lint": "eslint src test",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/main.js"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.1.2",
    "eslint": "^10.8.0",
    "typescript": "6.0.3",
    "typescript-eslint": "^8.66.0",
    "vitest": "^4.1.10"
  }
}
