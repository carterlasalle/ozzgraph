guards:
  secrets: true
  lint: true
  tests: true
  test_mode: full
  test_command: uv run --group dev pytest -x --tb=short
  test_timeout: 300
  static_analysis: false
evaluator:
  max_iterations: 60
  max_time: 20m
  max_input_tokens: 1M
  max_output_tokens: 400k
  static_analysis_diagnostics: false
defaults:
  model: deepseek-v4-flash
  api_key_env: GITREINS_LLM_API_KEY
history:
  enabled: true
  storage: git
  max_verdicts: 1000
